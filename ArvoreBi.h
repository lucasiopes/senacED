#include <stdio.h>
#include <stdlib.h>

typedef struct N{
    int n;
    struct N *esq, *dir;
} node;

int nodes = 0;
node *raiz = NULL;

int inserirABI(node *N, int n_in){
    if(raiz->esq != NULL){
        if(raiz->dir != NULL){
            
        }
    }
}